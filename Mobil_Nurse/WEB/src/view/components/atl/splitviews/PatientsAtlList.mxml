<?xml version="1.0" encoding="utf-8"?>
<s:View xmlns:fx="http://ns.adobe.com/mxml/2009" 
		xmlns:s="library://ns.adobe.com/flex/spark" 
		title="ATS"
		creationComplete="creationCompleteHandler(event)"
		initialize="initializeHandler(event)">
	
	<fx:Script>
		<![CDATA[
			import mx.events.FlexEvent;
			
			import spark.components.ViewNavigator;
			
			public static const NAME:String = 'PatientsAtlList';
			
			public static const CHANGE_STATE:String = NAME + "ChangeState";
			
			public function changeState(event:Event):void{
				dispatchEvent( new Event( CHANGE_STATE, event.target ) );
			}
			
			protected function creationCompleteHandler(event:FlexEvent):void
			{
				this.actionBarVisible = false;
			}
			
			protected function initializeHandler(event:FlexEvent):void
			{
				this.id = NAME;
				//AppFacade.getInstance().registerMediator(new PatientsAtlListMediator(this));
				
			}
			
			public function getSelectedState():void
			{
				trace("getSelectedState##########################");
				/*
				switch(list.selectedItem){
				case communication:
				break;
				case move:
				trace("move");
				break;
				case vital:
				break;
				case feeding:
				break;
				case excrements:
				break;
				case dressUp:
				break;
				case hygiene:
				break;
				case safety:
				break;
				case religion:
				break;
				case sozialize:
				break;
				}*/
				
			}
			
			public static function getBla():void{
				trace("schiess mich tot");
			}
			
		]]>
	</fx:Script>
	
	<fx:Declarations>
		<!-- Platzieren Sie nichtvisuelle Elemente (z. B. Dienste, Wertobjekte) hier -->
	</fx:Declarations>
	
	<s:List id="list" width="100%" height="100%" click="changeState(event);" >
		<s:dataProvider>
			<s:ArrayCollection>
				<s:Label id="communication" text="Communication" />
				<s:Label id="motion" text="Motion"/>
				<s:Label id="vital" text="Hygiene"/>
				<s:Label id="feeding" text="Feeding"/>
				<s:Label id="excrements" text="Exrements"/>
				<s:Label id="dressUp" text="Dress Up"/>
				<s:Label id="hygiene" text="Hygiene"/>
				<s:Label id="safety" text="Safety"/>
				<s:Label id="religion" text="Religion"/>
				<s:Label id="sozialize" text="Sozial"/>
				
			</s:ArrayCollection>
		</s:dataProvider>
	</s:List>
	
</s:View>
